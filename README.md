# Iconive Hair Wigs

Next.js (App Router) storefront for Iconive Hair Wigs.

## Stack

- Next.js App Router + TypeScript
- Tailwind CSS v4 + shadcn/ui + Lucide
- TanStack Query v5 + Axios
- Zustand v5 (shared client state)
- React Hook Form + Zod

## Getting started

```bash
npm install --legacy-peer-deps
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Scripts

- `npm run dev` — development server
- `npm run build` — production build
- `npm run start` — start production server
- `npm run lint` — ESLint

## Architecture

See `.cursor/rules/project-structure.mdc` for directory layout and data-layer conventions.
# iconive-frontend
